version: 2.1
jobs:
  build_and_deploy:
    docker:
      - image: circleci/node:12.22.7-bullseye-browsers-legacy
    steps:
      - checkout
      - run: echo "npm installing"
      - run: npm install
      - run: echo "adding git configs"
      - run: 
        name: configuring git
        command: |
          git config user.email "badrri1995@gmail.com"
          git config user.name "ci-build-bot"
      - run: echo "deploying to github-pages"
      - run: npm run deploy
workflows:
  version: 2
  build_app:
    jobs:
      - build_and_deploy